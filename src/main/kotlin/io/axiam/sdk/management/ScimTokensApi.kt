// @generated by scripts/gen_management.py from management-registry.json and
// openapi.json. DO NOT EDIT -- your changes will be overwritten. Regenerate with
// `python3 scripts/gen_management.py`; CI verifies the committed output is current.
package io.axiam.sdk.management

import io.axiam.sdk.internal.ManagementTransport
import io.axiam.sdk.management.models.CreateScimTokenRequest
import io.axiam.sdk.management.models.CreateScimTokenResponse
import io.axiam.sdk.management.models.ScimTokenResponse
import java.util.UUID

/**
 * The scim_tokens management operations.
 *
 * A view over the client that created it, not a connection: constructing one performs no I/O
 * (§27.2 rule 1), so there is nothing to cache and nothing to close.
 */
class ScimTokensApi internal constructor(
    private val transport: ManagementTransport,
    private val scope: NamespaceScope = NamespaceScope.inherited(),
) {

    /**
     * Issues `GET /api/v1/scim-tokens`.
     *
     * @return the server response
     */
    suspend fun list(): List<ScimTokenResponse> {
        val path = "/api/v1/scim-tokens"
        val node = transport.send(
            operation = "scim_tokens.list",
            method = "GET",
            pathTemplate = "/api/v1/scim-tokens",
            path = path,
        )
        return ManagementSupport.decodeList("scim_tokens.list", ScimTokenResponse.serializer(), node)
    }

    /**
     * Issues `POST /api/v1/scim-tokens`.
     *
     * Not retried: §27.4 rule 8 makes every write on this surface single-shot, including the ones
     * that look idempotent.
     *
     * @param body the request body
     * @return the server response
     */
    suspend fun create(body: CreateScimTokenRequest): CreateScimTokenResponse {
        val path = "/api/v1/scim-tokens"
        val payload = ManagementSupport.encodeBody("scim_tokens.create", CreateScimTokenRequest.serializer(), body)
        val node = transport.send(
            operation = "scim_tokens.create",
            method = "POST",
            pathTemplate = "/api/v1/scim-tokens",
            path = path, body = payload,
        )
        return ManagementSupport.decode("scim_tokens.create", CreateScimTokenResponse.serializer(), node)
    }

    /**
     * Revokes rather than deletes: the row is what lets an operator later answer "was this
     * credential still live on the day of the incident?", and a deleted row answers nothing.
     *
     * Issues `DELETE /api/v1/scim-tokens/{id}`.
     *
     * Not retried: §27.4 rule 8 makes every write on this surface single-shot, including the ones
     * that look idempotent.
     *
     * @param id the id to address
     */
    suspend fun revoke(id: UUID) {
        val path = "/api/v1/scim-tokens/${id}"
        transport.send(
            operation = "scim_tokens.revoke",
            method = "DELETE",
            pathTemplate = "/api/v1/scim-tokens/{id}",
            path = path,
        )
    }
}
