// @generated by scripts/gen_management.py from management-registry.json and
// openapi.json. DO NOT EDIT -- your changes will be overwritten. Regenerate with
// `python3 scripts/gen_management.py`; CI verifies the committed output is current.
package io.axiam.sdk.management

import io.axiam.sdk.internal.ManagementTransport
import io.axiam.sdk.management.models.CreateTenantRequest
import io.axiam.sdk.management.models.Tenant
import io.axiam.sdk.management.models.UpdateTenant
import java.util.UUID

/**
 * The tenants management operations.
 *
 * A view over the client that created it, not a connection: constructing one performs no I/O
 * (§27.2 rule 1), so there is nothing to cache and nothing to close.
 */
class TenantsApi internal constructor(
    private val transport: ManagementTransport,
    private val scope: NamespaceScope = NamespaceScope.inherited(),
) {

    /**
     * Returns a handle addressing [orgId] instead of the client's organization.
     *
     * A NEW handle: this one is unchanged, so a scoped call cannot leak into unrelated code
     * holding the same reference (§27.4 rule 3).
     *
     * @param orgId the organization to address
     * @return a TenantsApi scoped to [orgId]
     */
    fun inOrg(orgId: UUID): TenantsApi =
        TenantsApi(transport, scope.copy(orgId = orgId))

    /**
     * Issues `GET /api/v1/organizations/{org_id}/tenants`.
     *
     * Paginated: `total` on the returned page is the size of the WHOLE set, not of this page
     * (§27.4 rule 4). Use `listAll(...)` to walk every page.
     *
     * @param page where to start and how many to ask for, or null for the server's default
     * @return the page of results
     */
    suspend fun list(page: PageRequest?): Page<Tenant> {
        val orgId = ManagementSupport.resolveOrg(transport, scope, "tenants.list")
        val path = "/api/v1/organizations/${orgId}/tenants"
        val query = ManagementSupport.pageQuery(emptyMap(), page)
        val node = transport.send(
            operation = "tenants.list",
            method = "GET",
            pathTemplate = "/api/v1/organizations/{org_id}/tenants",
            path = path, query = query,
        )
        return ManagementSupport.decodePage("tenants.list", Tenant.serializer(), node)
    }

    /**
     * Walks `list` to exhaustion and returns every item (§27.4 rule 4).
     *
     * Stops on an empty page even when the server's `total` disagrees, so a misreporting server
     * costs one wasted request rather than an unbounded loop.
     *
     * @param start the first window to request, or null for the server's default
     * @return every item, in the server's order
     */
    suspend fun listAll(start: PageRequest? = null): List<Tenant> =
        ManagementSupport.collectPages(start) { list(it) }

    /**
     * Issues `POST /api/v1/organizations/{org_id}/tenants`.
     *
     * Not retried: §27.4 rule 8 makes every write on this surface single-shot, including the ones
     * that look idempotent.
     *
     * @param body the request body
     * @return the server response
     */
    suspend fun create(body: CreateTenantRequest): Tenant {
        val orgId = ManagementSupport.resolveOrg(transport, scope, "tenants.create")
        val path = "/api/v1/organizations/${orgId}/tenants"
        val payload = ManagementSupport.encodeBody("tenants.create", CreateTenantRequest.serializer(), body)
        val node = transport.send(
            operation = "tenants.create",
            method = "POST",
            pathTemplate = "/api/v1/organizations/{org_id}/tenants",
            path = path, body = payload,
        )
        return ManagementSupport.decode("tenants.create", Tenant.serializer(), node)
    }

    /**
     * Issues `GET /api/v1/organizations/{org_id}/tenants/{tenant_id}`.
     *
     * @param tenantId the tenant id to address
     * @return the server response
     */
    suspend fun get(tenantId: UUID): Tenant {
        val orgId = ManagementSupport.resolveOrg(transport, scope, "tenants.get")
        val path = "/api/v1/organizations/${orgId}/tenants/${tenantId}"
        val node = transport.send(
            operation = "tenants.get",
            method = "GET",
            pathTemplate = "/api/v1/organizations/{org_id}/tenants/{tenant_id}",
            path = path,
        )
        return ManagementSupport.decode("tenants.get", Tenant.serializer(), node)
    }

    /**
     * Issues `PUT /api/v1/organizations/{org_id}/tenants/{tenant_id}`.
     *
     * A SPARSE update: the properties you leave unset are omitted from the wire body entirely and
     * left unchanged (§27.4 rule 5).
     *
     * Not retried: §27.4 rule 8 makes every write on this surface single-shot, including the ones
     * that look idempotent.
     *
     * @param tenantId the tenant id to address
     * @param body the request body
     * @return the server response
     */
    suspend fun update(tenantId: UUID, body: UpdateTenant): Tenant {
        val orgId = ManagementSupport.resolveOrg(transport, scope, "tenants.update")
        val path = "/api/v1/organizations/${orgId}/tenants/${tenantId}"
        val payload = ManagementSupport.encodeBody("tenants.update", UpdateTenant.serializer(), body)
        val node = transport.send(
            operation = "tenants.update",
            method = "PUT",
            pathTemplate = "/api/v1/organizations/{org_id}/tenants/{tenant_id}",
            path = path, body = payload,
        )
        return ManagementSupport.decode("tenants.update", Tenant.serializer(), node)
    }

    /**
     * Issues `DELETE /api/v1/organizations/{org_id}/tenants/{tenant_id}`.
     *
     * Not retried: §27.4 rule 8 makes every write on this surface single-shot, including the ones
     * that look idempotent.
     *
     * @param tenantId the tenant id to address
     */
    suspend fun delete(tenantId: UUID) {
        val orgId = ManagementSupport.resolveOrg(transport, scope, "tenants.delete")
        val path = "/api/v1/organizations/${orgId}/tenants/${tenantId}"
        transport.send(
            operation = "tenants.delete",
            method = "DELETE",
            pathTemplate = "/api/v1/organizations/{org_id}/tenants/{tenant_id}",
            path = path,
        )
    }
}
