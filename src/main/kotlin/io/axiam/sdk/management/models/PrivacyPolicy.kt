// @generated by scripts/gen_management.py from management-registry.json and
// openapi.json. DO NOT EDIT -- your changes will be overwritten. Regenerate with
// `python3 scripts/gen_management.py`; CI verifies the committed output is current.
package io.axiam.sdk.management.models

import kotlinx.serialization.SerialName
import kotlinx.serialization.Serializable

/**
 * Data-retention rules that apply after a subject asks to be erased.
 *
 * @property deletionGracePeriodDays How long a requested account erasure stays cancellable
 *     before the purge runs, in days. The window exists so an erasure triggered by mistake, or
 *     under coercion, can be undone — `POST /api/v1/auth/account/delete/cancel` works for exactly
 *     this long. It was fixed at 30 days in the handler, which meant the "cancel a pending
 *     deletion" control in the admin UI referred to a duration no operator could see or change.
 *     Shorter is the more restrictive direction, so a tenant may lower it and not raise it: it is
 *     time spent holding data the subject has already asked to have erased, and GDPR Art. 17(1)
 *     asks for that to be "without undue delay". The upper bound of 90 days is where Art. 12(3)'s
 *     one-month response deadline plus its two-month extension for complex cases runs out;
 *     anything past 30 wants a reason recorded.
 */
@Serializable
data class PrivacyPolicy(
    @SerialName("deletion_grace_period_days") val deletionGracePeriodDays: Int,
)
