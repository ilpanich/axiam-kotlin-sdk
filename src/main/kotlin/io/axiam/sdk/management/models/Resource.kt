// @generated by scripts/gen_management.py from management-registry.json and
// openapi.json. DO NOT EDIT -- your changes will be overwritten. Regenerate with
// `python3 scripts/gen_management.py`; CI verifies the committed output is current.
package io.axiam.sdk.management.models

import io.axiam.sdk.management.InstantSerializer
import io.axiam.sdk.management.UuidSerializer
import java.time.Instant
import java.util.UUID
import kotlinx.serialization.SerialName
import kotlinx.serialization.Serializable
import kotlinx.serialization.json.JsonElement

/**
 * The Resource schema from the server's OpenAPI document.
 *
 * @property createdAt the server's created_at field
 * @property id the server's id field
 * @property metadata the server's metadata field
 * @property name the server's name field
 * @property parentId Parent resource ID for hierarchical organization. `None` for root
 *     resources.
 * @property resourceType The type of resource (e.g., `project`, `service`, `endpoint`).
 * @property tenantId the server's tenant_id field
 * @property umaRegisteredBy The `client_id` that registered this resource through the UMA
 *     Protection API (X2), or `None` for a resource created any other way. Read-only from every
 *     ordinary path: &#91;`UpdateResource`&#93; cannot set it and &#91;`CreateResource`&#93;
 *     cannot either, so the only writer is the resource-registration handler. That is deliberate —
 *     the field backs a provenance badge in the admin UI, and a provenance marker anyone can write
 *     is decoration that reads like evidence.
 * @property updatedAt the server's updated_at field
 */
@Serializable
data class Resource(
    @SerialName("created_at") val createdAt: @Serializable(with = InstantSerializer::class) Instant,
    @SerialName("id") val id: @Serializable(with = UuidSerializer::class) UUID,
    @SerialName("metadata") val metadata: JsonElement,
    @SerialName("name") val name: String,
    @SerialName("parent_id") val parentId: @Serializable(with = UuidSerializer::class) UUID? = null,
    @SerialName("resource_type") val resourceType: String,
    @SerialName("tenant_id") val tenantId: @Serializable(with = UuidSerializer::class) UUID,
    @SerialName("uma_registered_by") val umaRegisteredBy: String? = null,
    @SerialName("updated_at") val updatedAt: @Serializable(with = InstantSerializer::class) Instant,
)
