// @generated by scripts/gen_management.py from management-registry.json and
// openapi.json. DO NOT EDIT -- your changes will be overwritten. Regenerate with
// `python3 scripts/gen_management.py`; CI verifies the committed output is current.
package io.axiam.sdk.management.models

import kotlinx.serialization.SerialName
import kotlinx.serialization.Serializable

/**
 * Whether, and on what terms, a `client_id` that is a URL is resolved by fetching the document it
 * names (T21.5, `draft-ietf-oauth-client-id-metadata-document`). # Why this is one nested policy
 * rather than nine fields Every field here is a term of a single decision — *do we fetch a
 * stranger's URL and make a client out of what comes back* — and none of them means anything
 * without &#91;`Self::enabled`&#93;. A tenant that states a CIMD posture states all of it; a
 * tenant that states none inherits its organization's whole posture rather than half of one, which
 * is the only merge that cannot produce a combination neither party wrote. # The two fields that
 * can widen, and the seven that cannot &#91;`Self::enabled`&#93; and &#91;`Self::allow_http`&#93;
 * are **ordered**: a tenant may turn either off but never on, exactly as `dynamic_registration`
 * may only move down its ladder. Everything else names *this tenant's* domains or *this tenant's*
 * bounds, and there is no sense in which one tenant's list of trusted publishers is stricter than
 * another's — the same argument &#91;`OidcPolicy`&#93; already makes for
 * `dcr_allowed_redirect_hosts`. # Every bound here is a security control
 * &#91;`Self::max_metadata_bytes`&#93;, &#91;`Self::min_cache_secs`&#93; and
 * &#91;`Self::max_cache_secs`&#93; are not tuning knobs. They are, respectively, the ceiling on a
 * read from an attacker-chosen URL, the floor under how often that read may be repeated, and the
 * ceiling on how long its result may be trusted. Each is clamped again in code against the three
 * constants above, so a settings row written by hand cannot lift them.
 *
 * Every property is optional, so this is a SPARSE body: what you leave unset is left unchanged,
 * and is omitted from the wire request entirely rather than sent as null (§27.4 rule 5). Naming
 * the properties you mean to change is the whole API — there is no builder because Kotlin does not
 * need one, and no way to accidentally send a field you did not name.
 *
 * @property allowHttp Permit an `http://` `client_id` and an `http://` fetch. **Development
 *     only, and it does more than its name says.** AXIAM's shared SSRF guard couples the scheme
 *     rule to the address rule — the same seam that lets an integration test point a fetch at a
 *     loopback mock server — so a tenant that allows `http` also allows the first hop to resolve
 *     to a private address. Redirect hops are validated strictly whatever this says, and a public
 *     deployment that sets it has removed the control that makes `169.254.169.254` unreachable.
 * @property confidentialOnly Refuse a document whose `token_endpoint_auth_method` is `none`.
 *     Off by default, because `none` is what every MCP desktop client is. A tenant that turns it
 *     on accepts only `private_key_jwt` documents, which is the posture for a deployment whose
 *     CIMD clients are servers rather than desktops.
 * @property enabled **Off unless somebody turns it on** (I1). With this `false`, a URL-shaped
 *     `client_id` is exactly today's unknown client: nothing is fetched, nothing is materialised,
 *     and the ordinary repository lookup answers as it always has.
 * @property maxCacheSecs The ceiling on a document's cache lifetime, in seconds. Clamped to
 *     &#91;`CIMD_MAX_CACHE_CEILING_SECS`&#93;.
 * @property maxMetadataBytes The hard cap on how many bytes of a document are read, before it
 *     is parsed. Clamped to &#91;`CIMD_MAX_METADATA_BYTES_CEILING`&#93;.
 * @property minCacheSecs The floor under a document's cache lifetime, in seconds. Clamped to
 *     &#91;`CIMD_MIN_CACHE_FLOOR_SECS`&#93;.
 * @property restrictSameDomain Require every `redirect_uris` host in the document to equal the
 *     host of the `client_id` URL itself. **On by default**, because the document says who the
 *     client is and a redirect to somewhere else is the one thing a stolen or mirrored document
 *     would want to change. It is turned **off** for the desktop MCP clients, whose callbacks are
 *     on loopback and therefore can never share a host with a `https://` `client_id`;
 *     `docs/admin/client-id-metadata-documents.md` says so and says why.
 * @property trustedClientIdDomains The hosts whose documents this tenant will fetch at all, as
 *     globs (`mcp.example.com`, or `*.example.com` for every host under one domain). **An empty
 *     list resolves nothing**, and enabling CIMD while it is empty is refused — see
 *     &#91;`validate_cimd_policy`&#93;. That is a deliberate departure from "a URL is a client
 *     identifier, so any URL will do": the fetch is triggered by an unauthenticated request naming
 *     the URL, so an unrestricted list is a request-forgery primitive offered to strangers,
 *     bounded only by the SSRF guard's address rules. Naming the publishers a tenant actually
 *     fronts costs one settings field and removes the class. **`*` is refused here, and so is a
 *     wildcard over a whole top-level domain** (`*.com`): both are the posture the empty list is
 *     refused for, spelled differently, and a control with no second control behind it cannot have
 *     a one-character bypass and still be the control. It is a floor and not a public-suffix check
 *     — `*.github.io` passes, and trusting shared hosting stays the operator's decision, bounded
 *     by the per-tenant quota rather than by this field. `*` remains valid in
 *     &#91;`CimdPolicy::trusted_redirect_domains`&#93;, whose entries are not fetch targets.
 * @property trustedRedirectDomains The hosts a document's `redirect_uris` may point at, as
 *     globs. The loopback hosts (`127.0.0.1`, `&#91;::1&#93;`, `localhost`) are always allowed,
 *     because RFC 8252 §7.3 is how every desktop MCP client receives its callback — so an empty
 *     list is not a refusal of everything, it is "loopback only", which is exactly the Claude Code
 *     and VS Code profile.
 */
@Serializable
data class CimdPolicy(
    @SerialName("allow_http") val allowHttp: Boolean? = null,
    @SerialName("confidential_only") val confidentialOnly: Boolean? = null,
    @SerialName("enabled") val enabled: Boolean? = null,
    @SerialName("max_cache_secs") val maxCacheSecs: Long? = null,
    @SerialName("max_metadata_bytes") val maxMetadataBytes: Long? = null,
    @SerialName("min_cache_secs") val minCacheSecs: Long? = null,
    @SerialName("restrict_same_domain") val restrictSameDomain: Boolean? = null,
    @SerialName("trusted_client_id_domains") val trustedClientIdDomains: List<String>? = null,
    @SerialName("trusted_redirect_domains") val trustedRedirectDomains: List<String>? = null,
)
