// @generated by scripts/gen_management.py from management-registry.json and
// openapi.json. DO NOT EDIT -- your changes will be overwritten. Regenerate with
// `python3 scripts/gen_management.py`; CI verifies the committed output is current.
package io.axiam.sdk.management.models

import kotlinx.serialization.KSerializer
import kotlinx.serialization.Serializable
import kotlinx.serialization.descriptors.PrimitiveKind
import kotlinx.serialization.descriptors.PrimitiveSerialDescriptor
import kotlinx.serialization.descriptors.SerialDescriptor
import kotlinx.serialization.encoding.Decoder
import kotlinx.serialization.encoding.Encoder

/**
 * Which security posture a client is registered under (X5.1). This is the FAPI "one switch". FAPI
 * 2.0 is not a single feature but a bundle of constraints — mandatory PAR, mandatory PKCE with
 * `S256`, mandatory strong client authentication, mandatory sender-constrained tokens, and a
 * refusal of every relaxation the base specs permit. Encoding them as one profile rather than a
 * dozen independent booleans means an operator cannot register a client that is *nearly* FAPI, and
 * a reviewer can answer "is this client financial-grade?" by reading one field. The same
 * philosophy as the rate-limit postures: ordinary clients see no behaviour change at all, because
 * &#91;`Standard`&#93;(Self::Standard) is the serde default and every row written before schema
 * v38 decodes to it.
 *
 * Each constant carries the spelling the server uses on the wire, so the Kotlin name can follow
 * Kotlin's conventions without changing what is sent.
 *
 * An **open** enum. A value this SDK's copy of the spec does not list decodes to
 * [ClientProfile.UNKNOWN] rather than failing the response it arrived in (CONTRACT §27.11 rule 1).
 * Its own wire spelling is the empty string, which no server value is: carrying an unrecognised
 * value back into an update is refused by the server rather than silently written as a spelling it
 * never used. A `when` over these constants needs an `UNKNOWN` branch.
 */
@Serializable(with = ClientProfile.Companion.Serializer::class)
enum class ClientProfile(val wire: String) {
    STANDARD("standard"),

    FAPI2("fapi2"),

    /** A value this SDK's copy of the spec does not list; see the type's doc. */
    UNKNOWN("");

    companion object {
        /**
         * Decodes an unrecognised value to [UNKNOWN] instead of throwing.
         *
         * kotlinx.serialization's generated enum serializer raises on a value
         * outside the constants, which fails the WHOLE response — not just the
         * field. That is the failure §27.11 rule 1 exists to prevent.
         */
        internal object Serializer : KSerializer<ClientProfile> {
            override val descriptor: SerialDescriptor =
                PrimitiveSerialDescriptor("io.axiam.sdk.management.models.ClientProfile", PrimitiveKind.STRING)

            override fun serialize(encoder: Encoder, value: ClientProfile) {
                encoder.encodeString(value.wire)
            }

            override fun deserialize(decoder: Decoder): ClientProfile {
                val raw = decoder.decodeString()
                return entries.firstOrNull { it != UNKNOWN && it.wire == raw } ?: UNKNOWN
            }
        }
    }
}
