// @generated by scripts/gen_management.py from management-registry.json and
// openapi.json. DO NOT EDIT -- your changes will be overwritten. Regenerate with
// `python3 scripts/gen_management.py`; CI verifies the committed output is current.
package io.axiam.sdk.management.models

import io.axiam.sdk.Sensitive
import kotlinx.serialization.Contextual
import kotlinx.serialization.SerialName
import kotlinx.serialization.Serializable

/**
 * Body of `POST /api/v1/organizations/{org_id}/ca-certificates/import`. Deliberately carries no
 * subject, validity window or key algorithm: all three are read out of the certificate itself. A
 * caller that could name them separately could name a subject the certificate does not have, and
 * AXIAM would enforce the claim while every relying party read the certificate.
 *
 * @property privateKeyPem PEM-encoded private key, if AXIAM is to take custody of it. Omit it
 *     to register the certificate as a trust anchor only. Write-only: it goes to the configured
 *     custodian on the way in and no endpoint returns it. -- SECRET: redacted from toString and
 *     from every rendering except the one request body it is sent in
 * @property publicCertPem PEM-encoded CA certificate.
 */
@Serializable
data class ImportCaCertificateRequest(
    @SerialName("private_key_pem") val privateKeyPem: @Contextual Sensitive<String>? = null,
    @SerialName("public_cert_pem") val publicCertPem: String,
)
