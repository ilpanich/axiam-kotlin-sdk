// @generated by scripts/gen_management.py from management-registry.json and
// openapi.json. DO NOT EDIT -- your changes will be overwritten. Regenerate with
// `python3 scripts/gen_management.py`; CI verifies the committed output is current.
package io.axiam.sdk.management.models

import kotlinx.serialization.SerialName
import kotlinx.serialization.Serializable

/**
 * FIDO certification level, as recorded in an MDS `statusReports` entry's `FIDO_CERTIFIED*`
 * status. Variant order is significant: `derive(PartialOrd, Ord)` gives `L1 < L1Plus < L2 < L2Plus
 * < L3 < L3Plus`, which `WebauthnAttestationPolicy::evaluate` (D8 step 9) relies on directly for
 * the `min_certification` boundary check (`entry_level >= policy_min`).
 *
 * Each constant carries the spelling the server uses on the wire, so the Kotlin name can follow
 * Kotlin's conventions without changing what is sent.
 */
@Serializable
enum class CertificationLevel {
    @SerialName("L1")
    L1,

    @SerialName("L1Plus")
    L1PLUS,

    @SerialName("L2")
    L2,

    @SerialName("L2Plus")
    L2PLUS,

    @SerialName("L3")
    L3,

    @SerialName("L3Plus")
    L3PLUS,
}
