// @generated by scripts/gen_management.py from management-registry.json and
// openapi.json. DO NOT EDIT -- your changes will be overwritten. Regenerate with
// `python3 scripts/gen_management.py`; CI verifies the committed output is current.
package io.axiam.sdk.management.models

import kotlinx.serialization.SerialName
import kotlinx.serialization.Serializable

/**
 * The CreateCaCertificateRequest schema from the server's OpenAPI document.
 *
 * @property intermediateSubject Common name for the signing intermediate — `vault_pki` custody
 *     only. Under that custodian Vault generates a root and an intermediate beneath it, and this
 *     names the second. Defaults to the root's subject with `Intermediate Authority` appended.
 *     Ignored by every other custodian, which produces one self-signed CA and has no second
 *     certificate to name.
 * @property intermediateValidityDays Validity of the signing intermediate, in days. Defaults
 *     to the root's.
 * @property issueFromRoot Issue leaves straight from the generated root instead of creating an
 *     intermediate. `vault_pki` custody only, and off by default. The default is the safer one: a
 *     root that signs only an intermediate can have that intermediate revoked and replaced without
 *     redistributing the trust anchor, and a root that signs leaves cannot.
 * @property keyAlgorithm the server's key_algorithm field
 * @property subject the server's subject field
 * @property validityDays Validity duration in days.
 */
@Serializable
data class CreateCaCertificateRequest(
    @SerialName("intermediate_subject") val intermediateSubject: String? = null,
    @SerialName("intermediate_validity_days") val intermediateValidityDays: Int? = null,
    @SerialName("issue_from_root") val issueFromRoot: Boolean? = null,
    @SerialName("key_algorithm") val keyAlgorithm: KeyAlgorithm,
    @SerialName("subject") val subject: String,
    @SerialName("validity_days") val validityDays: Int,
)
