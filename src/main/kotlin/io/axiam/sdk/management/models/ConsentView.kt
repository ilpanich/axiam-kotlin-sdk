// @generated by scripts/gen_management.py from management-registry.json and
// openapi.json. DO NOT EDIT -- your changes will be overwritten. Regenerate with
// `python3 scripts/gen_management.py`; CI verifies the committed output is current.
package io.axiam.sdk.management.models

import io.axiam.sdk.management.InstantSerializer
import java.time.Instant
import kotlinx.serialization.SerialName
import kotlinx.serialization.Serializable

/**
 * One consent record, as the subject sees it.
 *
 * @property acceptedAt the server's accepted_at field
 * @property consentType What was consented to, e.g. `terms_of_service` or
 *     `oidc_scope_release:<client_id>`.
 * @property version The document version or, for a scope release, the consented scopes.
 * @property withdrawable Whether this record can be withdrawn here. `false` for
 *     `terms_of_service`: withdrawing it is not a consent operation but an erasure, and it has its
 *     own endpoint with its own grace period. Reported rather than silently absent so the
 *     self-service page can show the record and explain it.
 */
@Serializable
data class ConsentView(
    @SerialName("accepted_at") val acceptedAt: @Serializable(with = InstantSerializer::class) Instant,
    @SerialName("consent_type") val consentType: String,
    @SerialName("version") val version: String,
    @SerialName("withdrawable") val withdrawable: Boolean,
)
