// @generated by scripts/gen_management.py from management-registry.json and
// openapi.json. DO NOT EDIT -- your changes will be overwritten. Regenerate with
// `python3 scripts/gen_management.py`; CI verifies the committed output is current.
package io.axiam.sdk.management

import io.axiam.sdk.internal.ManagementTransport
import io.axiam.sdk.management.models.CreatePermissionRequest
import io.axiam.sdk.management.models.Permission
import io.axiam.sdk.management.models.UpdatePermissionRequest
import java.util.UUID

/**
 * The permissions management operations.
 *
 * A view over the client that created it, not a connection: constructing one performs no I/O
 * (§27.2 rule 1), so there is nothing to cache and nothing to close.
 */
class PermissionsApi internal constructor(
    private val transport: ManagementTransport,
    private val scope: NamespaceScope = NamespaceScope.inherited(),
) {

    /**
     * Issues `GET /api/v1/permissions`.
     *
     * Paginated: `total` on the returned page is the size of the WHOLE set, not of this page
     * (§27.4 rule 4). Use `listAll(...)` to walk every page.
     *
     * @param page where to start and how many to ask for, or null for the server's default
     * @return the page of results
     */
    suspend fun list(page: PageRequest?): Page<Permission> {
        val path = "/api/v1/permissions"
        val query = ManagementSupport.pageQuery(emptyMap(), page)
        val node = transport.send(
            operation = "permissions.list",
            method = "GET",
            pathTemplate = "/api/v1/permissions",
            path = path, query = query,
        )
        return ManagementSupport.decodePage("permissions.list", Permission.serializer(), node)
    }

    /**
     * Walks `list` to exhaustion and returns every item (§27.4 rule 4).
     *
     * Stops on an empty page even when the server's `total` disagrees, so a misreporting server
     * costs one wasted request rather than an unbounded loop.
     *
     * @param start the first window to request, or null for the server's default
     * @return every item, in the server's order
     */
    suspend fun listAll(start: PageRequest? = null): List<Permission> =
        ManagementSupport.collectPages(start) { list(it) }

    /**
     * Issues `POST /api/v1/permissions`.
     *
     * Not retried: §27.4 rule 8 makes every write on this surface single-shot, including the ones
     * that look idempotent.
     *
     * @param body the request body
     * @return the server response
     */
    suspend fun create(body: CreatePermissionRequest): Permission {
        val path = "/api/v1/permissions"
        val payload = ManagementSupport.encodeBody("permissions.create", CreatePermissionRequest.serializer(), body)
        val node = transport.send(
            operation = "permissions.create",
            method = "POST",
            pathTemplate = "/api/v1/permissions",
            path = path, body = payload,
        )
        return ManagementSupport.decode("permissions.create", Permission.serializer(), node)
    }

    /**
     * Issues `GET /api/v1/permissions/{permission_id}`.
     *
     * @param permissionId the permission id to address
     * @return the server response
     */
    suspend fun get(permissionId: UUID): Permission {
        val path = "/api/v1/permissions/${permissionId}"
        val node = transport.send(
            operation = "permissions.get",
            method = "GET",
            pathTemplate = "/api/v1/permissions/{permission_id}",
            path = path,
        )
        return ManagementSupport.decode("permissions.get", Permission.serializer(), node)
    }

    /**
     * Issues `PUT /api/v1/permissions/{permission_id}`.
     *
     * A SPARSE update: the properties you leave unset are omitted from the wire body entirely and
     * left unchanged (§27.4 rule 5).
     *
     * Not retried: §27.4 rule 8 makes every write on this surface single-shot, including the ones
     * that look idempotent.
     *
     * @param permissionId the permission id to address
     * @param body the request body
     * @return the server response
     */
    suspend fun update(permissionId: UUID, body: UpdatePermissionRequest): Permission {
        val path = "/api/v1/permissions/${permissionId}"
        val payload = ManagementSupport.encodeBody("permissions.update", UpdatePermissionRequest.serializer(), body)
        val node = transport.send(
            operation = "permissions.update",
            method = "PUT",
            pathTemplate = "/api/v1/permissions/{permission_id}",
            path = path, body = payload,
        )
        return ManagementSupport.decode("permissions.update", Permission.serializer(), node)
    }

    /**
     * Issues `DELETE /api/v1/permissions/{permission_id}`.
     *
     * Not retried: §27.4 rule 8 makes every write on this surface single-shot, including the ones
     * that look idempotent.
     *
     * @param permissionId the permission id to address
     */
    suspend fun delete(permissionId: UUID) {
        val path = "/api/v1/permissions/${permissionId}"
        transport.send(
            operation = "permissions.delete",
            method = "DELETE",
            pathTemplate = "/api/v1/permissions/{permission_id}",
            path = path,
        )
    }
}
