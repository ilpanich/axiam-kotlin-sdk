// @generated by scripts/gen_management.py from management-registry.json and
// openapi.json. DO NOT EDIT -- your changes will be overwritten. Regenerate with
// `python3 scripts/gen_management.py`; CI verifies the committed output is current.
package io.axiam.sdk.management

import io.axiam.sdk.internal.ManagementTransport
import io.axiam.sdk.management.models.AddMemberRequest
import io.axiam.sdk.management.models.CreateGroupRequest
import io.axiam.sdk.management.models.Group
import io.axiam.sdk.management.models.RoleAssignment
import io.axiam.sdk.management.models.UpdateGroup
import io.axiam.sdk.management.models.UserResponse
import java.util.UUID

/**
 * The groups management operations.
 *
 * A view over the client that created it, not a connection: constructing one performs no I/O
 * (§27.2 rule 1), so there is nothing to cache and nothing to close.
 */
class GroupsApi internal constructor(
    private val transport: ManagementTransport,
    private val scope: NamespaceScope = NamespaceScope.inherited(),
) {

    /**
     * Issues `GET /api/v1/groups`.
     *
     * Paginated: `total` on the returned page is the size of the WHOLE set, not of this page
     * (§27.4 rule 4). Use `listAll(...)` to walk every page.
     *
     * @param page where to start and how many to ask for, or null for the server's default
     * @return the page of results
     */
    suspend fun list(page: PageRequest?): Page<Group> {
        val path = "/api/v1/groups"
        val query = ManagementSupport.pageQuery(emptyMap(), page)
        val node = transport.send(
            operation = "groups.list",
            method = "GET",
            pathTemplate = "/api/v1/groups",
            path = path, query = query,
        )
        return ManagementSupport.decodePage("groups.list", Group.serializer(), node)
    }

    /**
     * Walks `list` to exhaustion and returns every item (§27.4 rule 4).
     *
     * Stops on an empty page even when the server's `total` disagrees, so a misreporting server
     * costs one wasted request rather than an unbounded loop.
     *
     * @param start the first window to request, or null for the server's default
     * @return every item, in the server's order
     */
    suspend fun listAll(start: PageRequest? = null): List<Group> =
        ManagementSupport.collectPages(start) { list(it) }

    /**
     * Issues `POST /api/v1/groups`.
     *
     * Not retried: §27.4 rule 8 makes every write on this surface single-shot, including the ones
     * that look idempotent.
     *
     * @param body the request body
     * @return the server response
     */
    suspend fun create(body: CreateGroupRequest): Group {
        val path = "/api/v1/groups"
        val payload = ManagementSupport.encodeBody("groups.create", CreateGroupRequest.serializer(), body)
        val node = transport.send(
            operation = "groups.create",
            method = "POST",
            pathTemplate = "/api/v1/groups",
            path = path, body = payload,
        )
        return ManagementSupport.decode("groups.create", Group.serializer(), node)
    }

    /**
     * Issues `GET /api/v1/groups/{group_id}`.
     *
     * @param groupId the group id to address
     * @return the server response
     */
    suspend fun get(groupId: UUID): Group {
        val path = "/api/v1/groups/${groupId}"
        val node = transport.send(
            operation = "groups.get",
            method = "GET",
            pathTemplate = "/api/v1/groups/{group_id}",
            path = path,
        )
        return ManagementSupport.decode("groups.get", Group.serializer(), node)
    }

    /**
     * Issues `PUT /api/v1/groups/{group_id}`.
     *
     * A SPARSE update: the properties you leave unset are omitted from the wire body entirely and
     * left unchanged (§27.4 rule 5).
     *
     * Not retried: §27.4 rule 8 makes every write on this surface single-shot, including the ones
     * that look idempotent.
     *
     * @param groupId the group id to address
     * @param body the request body
     * @return the server response
     */
    suspend fun update(groupId: UUID, body: UpdateGroup): Group {
        val path = "/api/v1/groups/${groupId}"
        val payload = ManagementSupport.encodeBody("groups.update", UpdateGroup.serializer(), body)
        val node = transport.send(
            operation = "groups.update",
            method = "PUT",
            pathTemplate = "/api/v1/groups/{group_id}",
            path = path, body = payload,
        )
        return ManagementSupport.decode("groups.update", Group.serializer(), node)
    }

    /**
     * Issues `DELETE /api/v1/groups/{group_id}`.
     *
     * Not retried: §27.4 rule 8 makes every write on this surface single-shot, including the ones
     * that look idempotent.
     *
     * @param groupId the group id to address
     */
    suspend fun delete(groupId: UUID) {
        val path = "/api/v1/groups/${groupId}"
        transport.send(
            operation = "groups.delete",
            method = "DELETE",
            pathTemplate = "/api/v1/groups/{group_id}",
            path = path,
        )
    }

    /**
     * Issues `GET /api/v1/groups/{group_id}/members`.
     *
     * Paginated: `total` on the returned page is the size of the WHOLE set, not of this page
     * (§27.4 rule 4). Use `list_membersAll(...)` to walk every page.
     *
     * @param groupId the group id to address
     * @param page where to start and how many to ask for, or null for the server's default
     * @return the page of results
     */
    suspend fun listMembers(groupId: UUID, page: PageRequest?): Page<UserResponse> {
        val path = "/api/v1/groups/${groupId}/members"
        val query = ManagementSupport.pageQuery(emptyMap(), page)
        val node = transport.send(
            operation = "groups.list_members",
            method = "GET",
            pathTemplate = "/api/v1/groups/{group_id}/members",
            path = path, query = query,
        )
        return ManagementSupport.decodePage("groups.list_members", UserResponse.serializer(), node)
    }

    /**
     * Walks `listMembers` to exhaustion and returns every item (§27.4 rule 4).
     *
     * Stops on an empty page even when the server's `total` disagrees, so a misreporting server
     * costs one wasted request rather than an unbounded loop.
     *
     * @param groupId the group id to address
     * @param start the first window to request, or null for the server's default
     * @return every item, in the server's order
     */
    suspend fun listMembersAll(groupId: UUID, start: PageRequest? = null): List<UserResponse> =
        ManagementSupport.collectPages(start) { listMembers(groupId, it) }

    /**
     * Issues `POST /api/v1/groups/{group_id}/members`.
     *
     * Not retried: §27.4 rule 8 makes every write on this surface single-shot, including the ones
     * that look idempotent.
     *
     * @param groupId the group id to address
     * @param body the request body
     */
    suspend fun addMember(groupId: UUID, body: AddMemberRequest) {
        val path = "/api/v1/groups/${groupId}/members"
        val payload = ManagementSupport.encodeBody("groups.add_member", AddMemberRequest.serializer(), body)
        transport.send(
            operation = "groups.add_member",
            method = "POST",
            pathTemplate = "/api/v1/groups/{group_id}/members",
            path = path, body = payload,
        )
    }

    /**
     * Issues `DELETE /api/v1/groups/{group_id}/members/{user_id}`.
     *
     * Not retried: §27.4 rule 8 makes every write on this surface single-shot, including the ones
     * that look idempotent.
     *
     * @param groupId the group id to address
     * @param userId the user id to address
     */
    suspend fun removeMember(groupId: UUID, userId: UUID) {
        val path = "/api/v1/groups/${groupId}/members/${userId}"
        transport.send(
            operation = "groups.remove_member",
            method = "DELETE",
            pathTemplate = "/api/v1/groups/{group_id}/members/{user_id}",
            path = path,
        )
    }

    /**
     * Lists a group's role assignments with the resource each is scoped to. Every member of the
     * group inherits these.
     *
     * Issues `GET /api/v1/groups/{group_id}/roles`.
     *
     * @param groupId the group id to address
     * @return the server response
     */
    suspend fun listRoles(groupId: UUID): List<RoleAssignment> {
        val path = "/api/v1/groups/${groupId}/roles"
        val node = transport.send(
            operation = "groups.list_roles",
            method = "GET",
            pathTemplate = "/api/v1/groups/{group_id}/roles",
            path = path,
        )
        return ManagementSupport.decodeList("groups.list_roles", RoleAssignment.serializer(), node)
    }
}
