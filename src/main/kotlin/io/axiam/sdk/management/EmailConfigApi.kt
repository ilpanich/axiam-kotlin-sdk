// @generated by scripts/gen_management.py from management-registry.json and
// openapi.json. DO NOT EDIT -- your changes will be overwritten. Regenerate with
// `python3 scripts/gen_management.py`; CI verifies the committed output is current.
package io.axiam.sdk.management

import io.axiam.sdk.internal.ManagementTransport
import io.axiam.sdk.management.models.EmailConfig
import io.axiam.sdk.management.models.EmailConfigOverride
import io.axiam.sdk.management.models.EmailTestResult
import io.axiam.sdk.management.models.SetOrgEmailConfig
import java.util.UUID

/**
 * The email_config management operations.
 *
 * A view over the client that created it, not a connection: constructing one performs no I/O
 * (§27.2 rule 1), so there is nothing to cache and nothing to close.
 */
class EmailConfigApi internal constructor(
    private val transport: ManagementTransport,
    private val scope: NamespaceScope = NamespaceScope.inherited(),
) {

    /**
     * Returns a handle addressing [orgId] instead of the client's organization.
     *
     * A NEW handle: this one is unchanged, so a scoped call cannot leak into unrelated code
     * holding the same reference (§27.4 rule 3).
     *
     * @param orgId the organization to address
     * @return a EmailConfigApi scoped to [orgId]
     */
    fun inOrg(orgId: UUID): EmailConfigApi =
        EmailConfigApi(transport, scope.copy(orgId = orgId))

    /**
     * Returns a handle addressing [tenantId] instead of the client's tenant.
     *
     * A NEW handle: this one is unchanged (§27.4 rule 3).
     *
     * @param tenantId the tenant to address
     * @return a EmailConfigApi scoped to [tenantId]
     */
    fun forTenant(tenantId: UUID): EmailConfigApi =
        EmailConfigApi(transport, scope.copy(tenantId = tenantId))

    /**
     * Issues `GET /api/v1/organizations/{org_id}/email-config`.
     *
     * @return the server response
     */
    suspend fun getOrg(): EmailConfig {
        val orgId = ManagementSupport.resolveOrg(transport, scope, "email_config.get_org")
        val path = "/api/v1/organizations/${orgId}/email-config"
        val node = transport.send(
            operation = "email_config.get_org",
            method = "GET",
            pathTemplate = "/api/v1/organizations/{org_id}/email-config",
            path = path,
        )
        return ManagementSupport.decode("email_config.get_org", EmailConfig.serializer(), node)
    }

    /**
     * Issues `PUT /api/v1/organizations/{org_id}/email-config`.
     *
     * A REPLACEMENT, not a patch: every field of the body is written, so read the current value
     * first and `copy()` the parts you mean to keep (§27.4 rule 5).
     *
     * Not retried: §27.4 rule 8 makes every write on this surface single-shot, including the ones
     * that look idempotent.
     *
     * @param body the request body
     * @return the server response
     */
    suspend fun setOrg(body: SetOrgEmailConfig): EmailConfig {
        val orgId = ManagementSupport.resolveOrg(transport, scope, "email_config.set_org")
        val path = "/api/v1/organizations/${orgId}/email-config"
        val payload = ManagementSupport.encodeBody("email_config.set_org", SetOrgEmailConfig.serializer(), body)
        val node = transport.send(
            operation = "email_config.set_org",
            method = "PUT",
            pathTemplate = "/api/v1/organizations/{org_id}/email-config",
            path = path, body = payload,
        )
        return ManagementSupport.decode("email_config.set_org", EmailConfig.serializer(), node)
    }

    /**
     * Issues `DELETE /api/v1/organizations/{org_id}/email-config`.
     *
     * Not retried: §27.4 rule 8 makes every write on this surface single-shot, including the ones
     * that look idempotent.
     */
    suspend fun deleteOrg() {
        val orgId = ManagementSupport.resolveOrg(transport, scope, "email_config.delete_org")
        val path = "/api/v1/organizations/${orgId}/email-config"
        transport.send(
            operation = "email_config.delete_org",
            method = "DELETE",
            pathTemplate = "/api/v1/organizations/{org_id}/email-config",
            path = path,
        )
    }

    /**
     * Issues `POST /api/v1/organizations/{org_id}/email-config/test`.
     *
     * Not retried: §27.4 rule 8 makes every write on this surface single-shot, including the ones
     * that look idempotent.
     *
     * @return the server response
     */
    suspend fun testOrg(): EmailTestResult {
        val orgId = ManagementSupport.resolveOrg(transport, scope, "email_config.test_org")
        val path = "/api/v1/organizations/${orgId}/email-config/test"
        val node = transport.send(
            operation = "email_config.test_org",
            method = "POST",
            pathTemplate = "/api/v1/organizations/{org_id}/email-config/test",
            path = path,
        )
        return ManagementSupport.decode("email_config.test_org", EmailTestResult.serializer(), node)
    }

    /**
     * Issues `GET /api/v1/tenants/{tenant_id}/email-config`.
     *
     * @return the server response
     */
    suspend fun getTenant(): EmailConfigOverride {
        val tenantId = ManagementSupport.resolveTenant(transport, scope, "email_config.get_tenant")
        val path = "/api/v1/tenants/${tenantId}/email-config"
        val node = transport.send(
            operation = "email_config.get_tenant",
            method = "GET",
            pathTemplate = "/api/v1/tenants/{tenant_id}/email-config",
            path = path,
        )
        return ManagementSupport.decode("email_config.get_tenant", EmailConfigOverride.serializer(), node)
    }

    /**
     * Issues `PUT /api/v1/tenants/{tenant_id}/email-config`.
     *
     * A SPARSE update: the properties you leave unset are omitted from the wire body entirely and
     * left unchanged (§27.4 rule 5).
     *
     * Not retried: §27.4 rule 8 makes every write on this surface single-shot, including the ones
     * that look idempotent.
     *
     * @param body the request body
     * @return the server response
     */
    suspend fun setTenant(body: EmailConfigOverride): EmailConfigOverride {
        val tenantId = ManagementSupport.resolveTenant(transport, scope, "email_config.set_tenant")
        val path = "/api/v1/tenants/${tenantId}/email-config"
        val payload = ManagementSupport.encodeBody("email_config.set_tenant", EmailConfigOverride.serializer(), body)
        val node = transport.send(
            operation = "email_config.set_tenant",
            method = "PUT",
            pathTemplate = "/api/v1/tenants/{tenant_id}/email-config",
            path = path, body = payload,
        )
        return ManagementSupport.decode("email_config.set_tenant", EmailConfigOverride.serializer(), node)
    }

    /**
     * Issues `DELETE /api/v1/tenants/{tenant_id}/email-config`.
     *
     * Not retried: §27.4 rule 8 makes every write on this surface single-shot, including the ones
     * that look idempotent.
     */
    suspend fun deleteTenant() {
        val tenantId = ManagementSupport.resolveTenant(transport, scope, "email_config.delete_tenant")
        val path = "/api/v1/tenants/${tenantId}/email-config"
        transport.send(
            operation = "email_config.delete_tenant",
            method = "DELETE",
            pathTemplate = "/api/v1/tenants/{tenant_id}/email-config",
            path = path,
        )
    }

    /**
     * Issues `POST /api/v1/tenants/{tenant_id}/email-config/test`.
     *
     * Not retried: §27.4 rule 8 makes every write on this surface single-shot, including the ones
     * that look idempotent.
     *
     * @return the server response
     */
    suspend fun testTenant(): EmailTestResult {
        val tenantId = ManagementSupport.resolveTenant(transport, scope, "email_config.test_tenant")
        val path = "/api/v1/tenants/${tenantId}/email-config/test"
        val node = transport.send(
            operation = "email_config.test_tenant",
            method = "POST",
            pathTemplate = "/api/v1/tenants/{tenant_id}/email-config/test",
            path = path,
        )
        return ManagementSupport.decode("email_config.test_tenant", EmailTestResult.serializer(), node)
    }
}
